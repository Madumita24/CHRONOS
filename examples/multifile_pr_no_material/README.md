# No-material-change PR fixture

This authored, never-executed fixture uses a real mapped DataHub model but
changes only SQL formatting/comments, schema documentation/key order, Python
comments, and Markdown. Expected disposition is `no_material_change`.
