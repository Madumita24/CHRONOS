# Conditional delete repair

The predecessor and repair proposals explicitly identify `order_total` as a
no-replacement deletion. The candidate removes only the exact list value from
the bounded quality configuration. Human review and Phase 7 consumer/runtime
validation remain mandatory.
